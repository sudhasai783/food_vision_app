# Food Vision Ultra (Advanced) — Streamlit Demo

Professional repo skeleton for a Food-101 classification demo.

## Structure
- `app.py` - Streamlit frontend that loads model and predicts on uploaded images.
- `src/` - reusable python modules (model loading, preprocessing, prediction).
- `models/` - put your `food101_best.pth` here (not included in repo by default).
- `assets/` - optional demo images or screenshots.
